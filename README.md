# Where are we gonna eat ?

## Development

* clone the project
* run: `npm install`
* remove the `.dist` in the `*.js.dist` files and fill what is missing (mostly keys for parse.com)
* run: 'ionic serve' to serve the app in a broswer or `ionic run android` to run it on Android
